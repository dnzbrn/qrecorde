import { json, requireSession, runtimeEnv } from "../../../../../lib/server";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const session=await requireSession(request);const{id:eventId}=await params;const owner=await runtimeEnv.DB.prepare("SELECT id FROM events WHERE id=? AND owner_id=?").bind(eventId,session.user.id).first();if(!owner)return json({error:"Evento não encontrado."},404);const count=await runtimeEnv.DB.prepare("SELECT COUNT(*) as total FROM sponsors WHERE event_id=?").bind(eventId).first<{total:number}>();if((count?.total||0)>=20)return json({error:"Limite de 20 patrocinadores atingido."},400);const body=await request.json() as {name?:string;tier?:string;tagline?:string};const sponsorId=crypto.randomUUID();await runtimeEnv.DB.prepare("INSERT INTO sponsors (id,event_id,name,tier,tagline,position,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?)").bind(sponsorId,eventId,body.name?.trim()||"Novo patrocinador",["master","sponsor","supporter"].includes(body.tier||"")?body.tier:"sponsor",body.tagline?.trim()||"",count?.total||0,Date.now(),Date.now()).run();return json({id:sponsorId},201);}catch(error){return error instanceof Response?error:json({error:"Não foi possível adicionar."},500);}}
